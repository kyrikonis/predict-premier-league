# Postgres connection string (Neon, provisioned via the Vercel dashboard in production)
DATABASE_URL=""

# football-data.org API token (free tier): https://www.football-data.org/client/register
FOOTBALL_DATA_API_KEY=""

# 32+ char random string used to encrypt the session cookie (no password auth, just username)
SESSION_SECRET=""

# Random string Vercel Cron sends as a Bearer token to authenticate scheduled job requests
CRON_SECRET=""
